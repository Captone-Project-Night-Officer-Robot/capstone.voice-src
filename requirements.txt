# ── PyTorch (CPU-only — required for MultilingualModel turn detector) ──────────
--extra-index-url https://download.pytorch.org/whl/cpu
torch

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# ── LiveKit agent + plugins ───────────────────────────────────────────────────
livekit-agents[openai,elevenlabs,silero,turn-detector,noise-cancellation]~=1.5.1
livekit-api>=0.7.0

# ── Config & validation ───────────────────────────────────────────────────────
pydantic>=2.7.0
pydantic-settings>=2.3.0

# ── Env ───────────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── HTTP client (for future tool calls to backend) ────────────────────────────
httpx>=0.27.0

# ── Optional: laptop-side audio listener (src/listen_local.py) ────────────────
# Plays TTS audio through the laptop's default output device when the Pi has
# no speaker. Needs system portaudio first:
#   macOS:  brew install portaudio
#   Linux:  sudo apt install libportaudio2
sounddevice>=0.4.6
